# future changes to this file are to be ignored via
#    git update-index --skip-worktree ./config.py
# 
# you may specify a username and password here which will be used in absence of 
# parameters passed to rpc_controller.py and those changes will be ignored by
# git in order to prevent accidental exposure. if changes are made to this file
# which should be updated within a single commit, use 
#    git update-index --no-skip-worktree ./config.py

rpc_info = {
    "username": "turkyrpc",
    "password": "turkypass",
    "port": 8332,
    "timeout": 30
}